import { pageMetadata } from "@/lib/seo";

export const metadata = pageMetadata({
  title: "Raksha Bandhan 2026 Special - Greetings, Wishes & Cards",
  description: "Celebrate the sibling bond for Raksha Bandhan 2026. Create custom greetings, greetings cards, and view subh mahurat timings.",
  path: "/rakshabandhan-2026",
});

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
